[project]
name = "web-search-mcp"
version = "0.1.0"
description = "Self-owned web search/fetch MCP — SearXNG first, cloud providers as fallback/diversity, quota-tracked"
requires-python = ">=3.12"
dependencies = [
    "mcp>=2,<3",
    "httpx>=0.28",
    "python-dotenv>=1.1",
    "youtube-transcript-api>=1.2.4",
    "curl-cffi>=0.16.3",
]

[tool.uv]
package = false
